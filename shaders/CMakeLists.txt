set(TARGET_NAME Shader)

file(GLOB_RECURSE SHADER_FILES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/*.hlsl" "${CMAKE_CURRENT_SOURCE_DIR}/*.hlsli")

SOURCE_GROUP("shaders" FILES ${SHADER_FILES})

add_custom_target(${TARGET_NAME} SOURCES ${SHADER_FILES})
